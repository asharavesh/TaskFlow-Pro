{
  "dependencies": {
    "node-cron": "^4.1.1",
    "react-icons": "^5.5.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --prefix frontend",
    "preview": "npm run serve --prefix frontend"
  }
}
